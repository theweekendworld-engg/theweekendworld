# Database
DATABASE_URL=postgresql://user:password@localhost:5432/theweekendworld?sslmode=disable

# NextAuth
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
# For local development
NEXTAUTH_URL=http://localhost:3000
# For production on Vercel, set to your production URL:
# NEXTAUTH_URL=https://your-app.vercel.app
# Or use your custom domain:
# NEXTAUTH_URL=https://yourdomain.com

# GitHub API (optional, but recommended for higher rate limits)
GITHUB_TOKEN=your_github_personal_access_token

# Admin Credentials (set initial admin user via script or manually)
# Password should be hashed using bcrypt before storing in database

